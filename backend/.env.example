OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash
PORT=3000

# Set NODE_ENV=production in hosted environments. The backend will refuse to
# start in production unless OPENROUTER_API_KEY, BACKEND_CLIENT_TOKEN, and
# ENFORCE_HTTPS=true are configured.
NODE_ENV=development

# Set this in production and pass the same value to Flutter as BACKEND_CLIENT_TOKEN.
BACKEND_CLIENT_TOKEN=

# Local development can stay false. Production behind HTTPS should set this true.
ENFORCE_HTTPS=false
TRUST_PROXY=false

# Comma-separated browser origins if a web client is added. Empty disables CORS browser access.
CORS_ALLOWED_ORIGINS=

# Request abuse controls.
REQUEST_BODY_LIMIT=12mb
MAX_SELECTED_FRAMES=3
MAX_ALL_FRAMES=30
MAX_BASE64_IMAGE_BYTES=5242880
MAX_USER_COMMAND_CHARS=500
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=20
DAILY_QUOTA_MAX_REQUESTS=300
